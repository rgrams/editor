
local Number = require "theme.widgets.properties.Number"

local M = {
	bool = Number,
	color = Number,
	font = Number,
	image = Number,
	number = Number,
	quad = Number,
	string = Number,
	vector2 = Number,
}

return M